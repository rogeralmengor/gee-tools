version: '3.8'

services:
  gee-tools:
    build: .
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests
    # Pass in your local GEE credentials so the container can authenticate
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
